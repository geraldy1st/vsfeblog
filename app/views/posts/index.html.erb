<h1>Blog Posts</h1>
<% @post.each do |post| %>
    <div class="card card-body ">
    <h3><%= post.title %></h3>
    <p><%= post.body %></p>
    <%= link_to "Read More", post_path(post), :class => 'btn btn-light' %>
    </div>
    <% end %>